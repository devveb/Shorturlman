create table short_url
(
	idx int auto_increment
		primary key,
	origin_url varchar(512) null,
	short_url varchar(32) null,
	hit_cnt int default 0 null,
	rdate datetime default current_timestamp() null
);

